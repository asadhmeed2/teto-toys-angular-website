services:
  server:
    build:
      context: .
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"

  admin:
    build:
      context: ../admin-teto-toys
    environment:
      NODE_ENV: development
    ports:
      - "4201:4201"
    restart: unless-stopped
